version: '3.8'

services:
  iex-api:
    build: .
    container_name: group05-iex-api
    ports:
      - "5000:5000"
    volumes:
      # Data + models persist on your disk, not inside container
      - ./data:/app/data
      - ./models:/app/models
      - ./app/static:/app/app/static
    env_file:
      # Reads your .env file — keys never baked into image
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
